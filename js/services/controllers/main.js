const productoContainer = document.querySelector ("[data-product]");

function createCard (mane, precio, imagen, id){
    const card = document.createElement("card");
    card.classlist.add("card")
}
